# ── NitroGen Game Voice Coach 依赖 ────────────────────────────────
# conda activate py311
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ── Web 框架 ─────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.2.2
python-multipart==0.0.9
websockets==16.0

# ── NitroGen 通信 ────────────────────────────────────────────────────
pyzmq>=25.0.0

# ── 视频 / 图像 ──────────────────────────────────────────────────────
opencv-python==4.10.0.84
pillow==12.2.0

# ── AI / LLM ─────────────────────────────────────────────────────────
anthropic>=0.25.0
openai==2.43.0
httpx>=0.27.0

# ── ASR（语音识别）────────────────────────────────────────────────────
faster-whisper==1.2.1
openai-whisper==20250625
ctranslate2==4.8.0

# ── TTS（语音合成）────────────────────────────────────────────────────
edge-tts==7.2.8
requests==2.34.2

# ── 音频 ─────────────────────────────────────────────────────────────
pydub==0.25.1
sounddevice==0.5.5
miniaudio==1.71

# ── 文本处理 ─────────────────────────────────────────────────────────
opencc-python-reimplemented==0.1.7

# ── SSH 隧道（NitroGen 远程连接）─────────────────────────────────────
paramiko>=3.4.0

# ── 科学计算（通常已随 torch 安装）───────────────────────────────────
numpy>=2.0.0

# ── PyTorch（ASR GPU 加速，CPU 用户可跳过）───────────────────────────
# torch / torchaudio 建议通过 conda 或官网安装，不写死版本
# conda install pytorch torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
