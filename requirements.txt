# ── 已在 live_vision 环境中（可能已有）──────────────────────────────
# fastapi, numpy, pillow, pydub, python-dotenv, python-multipart

# ── Web 服务（必填：缺 websockets 时 /ws 会 404，探针 WebSocket 步骤失败）──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# ── 需要额外安装 ────────────────────────────────────────────────────
# pip install -r requirements.txt

# NitroGen 通信
pyzmq>=25.0.0

# 视频处理
opencv-python>=4.9.0

# AI
anthropic>=0.25.0
openai-whisper>=20231117
httpx>=0.27.0
paramiko>=3.4.0

# TTS
edge-tts>=6.1.9

# 音频播放（任选其一）
pygame>=2.5.0       # 推荐，简单可靠
# pyaudio>=0.2.14   # 备选

# 异步文件
aiofiles>=23.0.0
